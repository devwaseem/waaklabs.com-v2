---
import { Image } from "astro:assets";
import LogoBlack from "../assets/images/logo-black.svg";
---

<nav class="mx-auto flex max-w-xl flex-row justify-between">
    <a
        href="/"
        class="flex flex-row items-center justify-between lg:justify-start">
        <Image
            src={LogoBlack}
            alt="Logo of waaklabs"
            loading="eager"
            width={50}
            height={50}
        />
    </a>
    <div class="flex flex-row items-center gap-6">
        <!-- <a href="#">Blog</a> -->
        <button
            @click="openChatWidget()"
            class="active:text-accent-400 group inline-flex h-8 items-center justify-center rounded-lg bg-black px-4 py-2 text-sm font-medium text-white transition-transform hover:border hover:border-black hover:bg-white hover:text-black focus:outline-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-black active:scale-95">
            Lets Talk 👋
        </button>
    </div>
</nav>
